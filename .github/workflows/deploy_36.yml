name: Deploy to .36
on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - 'Controllers/**'
      - '.github/workflows/deploy_36.yml'
jobs:
  deploy:
    runs-on: [self-hosted, Windows, X64, iis]
    steps:
      - uses: actions/checkout@v4

      - name: Sync controllers + copy DLLs from FMS_PUTWAY + deploy
        shell: powershell
        run: |
          $VSPROJ = "D:\V2 Projects\Vendor_SRM_Routing_Application"
          $LIVE   = "D:\V2 Published\FMS_PUTWAY\bin"
          $OUT    = "C:\V2RfcTest"
          $MSBUILD = "C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe"
          $REPO   = "C:\actions-runner\_work\rfc-api\rfc-api"

          Write-Host "=== Step 1: Sync controllers from GitHub to VS project ==="
          robocopy "$REPO\Controllers" "$VSPROJ\Controllers" /E /IS /IT /NFL /NDL
          Write-Host "Controllers synced"

          Write-Host "=== Step 2: Build ==="
          if (-not (Test-Path "$OUT\bin")) { New-Item "$OUT\bin" -ItemType Directory -Force | Out-Null }
          & $MSBUILD "$VSPROJ\Vendor_SRM_Routing_Application.csproj" /p:Configuration=Release /p:Platform=AnyCPU /p:OutputPath="$OUT\bin\" /p:VisualStudioVersion=17.0 /nologo /verbosity:minimal
          if ($LASTEXITCODE -ne 0) { Write-Host "BUILD FAILED"; exit 1 }
          Write-Host "BUILD SUCCESS"

          Write-Host "=== Step 3: Copy ALL DLLs from live FMS_PUTWAY (correct versions) ==="
          $dllCount = 0
          Get-ChildItem $LIVE -Filter "*.dll" | Where-Object {$_.Name -ne "Vendor_SRM_Routing_Application.dll"} | ForEach-Object {
            Copy-Item $_.FullName "$OUT\bin\" -Force
            $dllCount++
          }
          Write-Host "Copied $dllCount DLLs from live app"
          Write-Host "Total in bin: $((Get-ChildItem "$OUT\bin" -Filter "*.dll").Count)"

          Write-Host "=== Step 4: Copy Global.asax ==="
          Copy-Item "$VSPROJ\Global.asax" "$OUT\Global.asax" -Force
          Write-Host "Global.asax: $(Test-Path "$OUT\Global.asax")"

          Write-Host "=== Step 5: Write Web.config with binding redirects ==="
          $cfg = @"
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <appSettings>
    <add key="Name" value="V2Dev" />
    <add key="AppServerHost" value="192.168.144.174" />
    <add key="Client" value="210" />
    <add key="User" value="POWERBI" />
    <add key="Password" value="India@123456" />
    <add key="SystemNumber" value="00" />
    <add key="Language" value="EN" />
  </appSettings>
  <system.web>
    <compilation debug="false" targetFramework="4.8" />
    <httpRuntime targetFramework="4.8" maxRequestLength="2147483647" />
    <customErrors mode="Off" />
  </system.web>
  <system.webServer>
    <handlers>
      <remove name="ExtensionlessUrlHandler-Integrated-4.0" />
      <add name="ExtensionlessUrlHandler-Integrated-4.0" path="*." verb="*" type="System.Web.Handlers.TransferRequestHandler" preCondition="integratedMode,runtimeVersionv4.0" />
    </handlers>
  </system.webServer>
  <runtime>
    <assemblyBinding xmlns="urn:schemas-microsoft-com:asm.v1">
      <dependentAssembly>
        <assemblyIdentity name="Microsoft.Web.Infrastructure" publicKeyToken="31bf3856ad364e35" />
        <bindingRedirect oldVersion="0.0.0.0-2.0.1.0" newVersion="2.0.1.0" />
      </dependentAssembly>
    </assemblyBinding>
  </runtime>
</configuration>
"@
          [System.IO.File]::WriteAllText("$OUT\Web.config", $cfg, [System.Text.Encoding]::UTF8)
          Write-Host "Web.config written"

          Write-Host "=== Step 6: Ensure site + pool exist ==="
          Import-Module WebAdministration -EA SilentlyContinue
          if (-not (Get-WebAppPoolState "V2RfcTestPool" -EA SilentlyContinue)) {
            New-WebAppPool "V2RfcTestPool" -EA SilentlyContinue
            Set-ItemProperty "IIS:\AppPools\V2RfcTestPool" managedRuntimeVersion "v4.0"
          }
          if (-not (Get-Website "V2RfcTest" -EA SilentlyContinue)) {
            New-Website -Name "V2RfcTest" -Port 9292 -PhysicalPath $OUT -ApplicationPool "V2RfcTestPool"
          }
          Restart-WebAppPool "V2RfcTestPool" -EA SilentlyContinue
          Start-Sleep 15

          Write-Host "=== Step 7: Test ==="
          try {
            $r = Invoke-WebRequest "http://localhost:9292/api/ZpoDdUpdRfc/Health" -UseBasicParsing -TimeoutSec 10
            Write-Host "HEALTH ZpoDdUpdRfc: $($r.StatusCode) $($r.Content)"
          } catch { Write-Host "HEALTH ERR: $($_.Exception.Message)" }

          try {
            $r2 = Invoke-WebRequest "http://localhost:9292/api/ZpoModificationRfc/Health" -UseBasicParsing -TimeoutSec 10
            Write-Host "HEALTH ZpoModificationRfc: $($r2.StatusCode) $($r2.Content)"
          } catch { Write-Host "HEALTH ERR2: $($_.Exception.Message)" }
